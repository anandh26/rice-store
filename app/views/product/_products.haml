%section.section.light-gray-bg.clearfix
  .container
    .row
      .col-md-8
        -if @products.length == 0
          .overlay-container
            %img.resize-coming-soon{:alt => "", :src => asset_path("coming-soon.jpg")}
        -else
          =render 'inner_products'
